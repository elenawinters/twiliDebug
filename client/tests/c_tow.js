// RegisterCommand('tow', (source, args) => {
//     tower = GetVehiclePedIsIn(PLAYER_PED(), false)
//     veh = parseInt(args[0])
//     if (!IsEntityAVehicle(tower) || !IsEntityAVehicle(veh)) { return;}
//     // i dont know if this actually runs, might remove the guard clause just in case
//     AttachVehicleToTowTruck(tower, veh, false, 0, 0, 0)
// });